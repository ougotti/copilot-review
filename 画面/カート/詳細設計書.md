# 詳細設計書 – カート画面

| 項目 | 内容 |
|------|------|
| 画面ID | SCR-003 |
| 画面名 | カート |
| 作成日 | 2026-04-08 |
| バージョン | 1.0 |

---

## 1. 処理フロー

```
[ページ表示]
    │
    ├─ セッションIDを取得
    │
    ├─ cart_items JOIN products で商品情報を取得
    │
    ├─ 在庫状況を最新に同期
    │       在庫 < カート数量の場合 → カート数量を在庫数に修正し警告表示
    │
    └─ 合計金額を算出して描画

[数量変更]
    │
    ├─ PATCH /api/cart/items/{id}  { quantity: N }
    │
    ├─ quantity = 0 の場合、DELETE 処理へ
    │
    ├─ 在庫チェック
    │
    ├─ cart_items を UPDATE
    │
    └─ 新しい合計金額をレスポンスに含めてフロントを更新

[商品削除]
    │
    ├─ DELETE /api/cart/items/{id}
    │
    └─ cart_items から該当レコードを DELETE
```

---

## 2. API定義

### GET /api/cart

**レスポンス (200 OK)**

```json
{
  "item_count": 2,
  "items": [
    {
      "cart_item_id": 42,
      "product_id": 1,
      "name": "サンプル商品A",
      "thumbnail_url": "/images/1.jpg",
      "unit_price": 2178,
      "quantity": 2,
      "subtotal": 4356,
      "stock": 5
    }
  ],
  "total": 4356
}
```

### PATCH /api/cart/items/{id}

```json
// Request
{ "quantity": 3 }

// Response 200
{ "subtotal": 6534, "cart_total": 6534 }
```

### DELETE /api/cart/items/{id}

```json
// Response 200
{ "message": "削除しました", "cart_total": 0 }
```

---

## 3. DB アクセス詳細

### カート一覧取得 SQL

```sql
SELECT
  ci.id         AS cart_item_id,
  p.id          AS product_id,
  p.name,
  p.thumbnail_url,
  FLOOR(p.price * 1.10) AS unit_price,
  ci.quantity,
  FLOOR(p.price * 1.10) * ci.quantity AS subtotal,
  p.stock
FROM cart_items ci
JOIN products p ON ci.product_id = p.id
WHERE ci.session_id = :session_id
ORDER BY ci.created_at ASC;
```

### 数量更新 SQL

```sql
UPDATE cart_items
SET quantity = :quantity, updated_at = NOW()
WHERE id = :cart_item_id AND session_id = :session_id;
```

---

## 4. エラーハンドリング

| エラー | 対応 |
|--------|------|
| 在庫不足（数量変更時） | エラーメッセージ表示、数量を在庫数に補正 |
| カートアイテムが存在しない | 404 レスポンス |
| DB エラー | 500 エラー画面、ログ出力 |

---

## 5. セキュリティ考慮

- cart_items の操作時は必ずセッションIDで所有者確認を行う（他ユーザーのカート操作を防止）
